# This file can remain empty.
# It signifies that the 'sql_interface' directory is a Python package,
# allowing you to import modules from it (e.g., from sql_interface.db_interface import SQLInterface).

# You could optionally import key classes here for easier access, e.g.:
# from .db_interface import SQLInterface
# from .query_manager import QueryManager
# from .output_formatter import OutputFormatter
# from .exceptions import QueryTemplateNotFoundError
# This would allow users to do `from sql_interface import SQLInterface` instead of the longer path.